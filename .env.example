# Optional non-secret path overrides. Prefer OS application-data defaults.
# SECUREDACT_MODEL_DIR=C:\absolute\user-owned\securedact-models
# SECUREDACT_SAFE_COPY_DIR=C:\absolute\user-owned\safe-copies

# Reduced regex/curated-rule mode is for synthetic development only and is not
# recommended for real sensitive data. The secure default is 1.
# SECUREDACT_REQUIRE_FLAIR=1

# Never place API keys, model credentials, prompts, mappings, or personal data in
# this file. Supported public models need no Hugging Face token.
